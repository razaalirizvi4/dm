# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Typical Development Commands

This repository currently does not contain source code, build scripts, or test suites. Consequently, there are no standard commands for building, linting, or testing.

If such tooling is added in the future, consider documenting commands here, for example:

- **Build**: `npm run build` or `make`
- **Lint**: `npm run lint` or `flake8 .`
- **Run Tests**: `npm test`, `pytest`, or `go test ./...`
- **Run a single test**: `npm test -- -t <test-name>` or `pytest tests/test_module.py::test_function`

## High‑Level Project Structure

- `AI_Powered_IoT_Network_Intrusion_Detection_Dataset.csv` – Sample dataset used for data‑science experiments.
- `.claude/agents/preprocessing-comparator.md` – Definition of the **preprocessing‑comparator** agent, which provides a framework for systematic data‑preprocessing comparisons. The agent description includes workflow, edge‑case guidance, expected output format, and memory‑update instructions.
- `.claude/agent-memory/` – Persistent memory directory for agents (currently empty).
- `CLAUDE.md` – This guidance file.

### Architecture Overview

The repository is organized around **data‑driven experiments** rather than a traditional application codebase. The primary artifact is a CSV dataset, and the only executable logic lives in Claude agents defined under `.claude/`. When working with this project, focus on:

1. **Data ingestion and preprocessing** – Use the `preprocessing‑comparator` agent to explore different pipelines on the CSV.
2. **Agent memory management** – Agents can store and retrieve reusable knowledge in `.claude/agent-memory/`.
3. **Extensibility** – If source code (e.g., Python scripts, notebooks) is added later, place them in a top‑level `src/` or `notebooks/` directory and update this CLAUDE.md accordingly.

## Adding Future Documentation

When new components are introduced (e.g., Python scripts, notebooks, a `src/` directory, CI pipelines), update this file to reflect:

- Build/run commands for the new language/toolchain.
- Directory layout and responsibilities of each top‑level folder.
- Any project‑specific conventions (naming, linting, testing frameworks).
- Relevant `.cursor` or GitHub Copilot instruction files if they appear.

---
*Generated by Claude Code based on the current repository contents.*